# Whoply — shopkeeper & wholesaler PWA (:7200 in dev)
# Base URL of the Whoply API, INCLUDING the /api path.
# These NEXT_PUBLIC_* values are baked in at build time.
NEXT_PUBLIC_API_URL=https://api.yourdomain.com/api
